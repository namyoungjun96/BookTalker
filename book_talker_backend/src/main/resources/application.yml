spring:
  profiles:
    active: local

  application:
    name: book_talker_backend

server:
  port: 8010
  forward-headers-strategy: native

management:
  endpoints:
    web:
      exposure:
        include: health
